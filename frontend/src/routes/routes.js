const apiRoutes = {
  loginPath: () => '/api/v1/login',
  signupPath: () => '/api/v1/signup',
  dataPath: () => '/api/v1/data',
};

const links = {
  main: () => '/',
  login: () => '/login',
  signup: () => '/signup',
};

export { apiRoutes, links };
